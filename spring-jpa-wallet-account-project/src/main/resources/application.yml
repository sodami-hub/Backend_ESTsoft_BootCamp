spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/wallet_account
    username : root
    password: admin
  flyway:
    enabled: false

logging:
  config: classpath:logback-local.xml